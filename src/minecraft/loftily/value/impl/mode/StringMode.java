package loftily.value.impl.mode;

/**
 * An implementation of {@link Mode} that can act as a mode.
 */
public final class StringMode extends Mode {
    public StringMode(String name) {
        super(name);
    }
}
